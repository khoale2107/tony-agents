"""
connector_example.py — SEAM gửi ZNS thật qua Zalo OA.
Copy thành connector.py rồi nhờ Claude Code điền theo tài khoản ZNS của bạn.
Chỉ cần:  send_zns(phone: str, text: str) -> None
"""
def send_zns(phone, text):
    # import finlib as fl; token = fl.cfg("ZALO_OA_TOKEN"); template_id = fl.cfg("ZNS_TEMPLATE_ID")
    # ...gọi API ZNS của Zalo OA...
    raise NotImplementedError("Nhờ Claude Code viết send_zns theo ZNS template của bạn.")
