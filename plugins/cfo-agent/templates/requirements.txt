# Bản MẶC ĐỊNH (dùng gói Claude Code) KHÔNG cần thư viện ngoài nào —
# chỉ dùng Python có sẵn + lệnh 'claude'.
#
# Chỉ khi bạn chọn chế độ API key (điền ANTHROPIC_API_KEY trong .env) thì cài:
#   pip install anthropic
