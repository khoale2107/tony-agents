# KHÔNG BAO GIỜ commit secret/data của học viên hay của mình.
.env
*.env
!.env.example
.venv/
__pycache__/
*.log
*.pyc

# Adapter ERP thật của học viên (có thể chứa token) — chỉ giữ bản .example
adapter_custom.py

# Kiến thức CSKH thật của học viên — chỉ giữ bản .example
knowledge.md

# Data thật (chỉ giữ file mẫu)
sample_output/

# File sinh khi chạy agent (dữ liệu/log cục bộ của học viên)
so_ke.csv
requests.csv
.tg_offset
agent.log
